#include "IRTDotOutputVisitor.h"
using namespace IRTree;

namespace GraphvizOutput {

	/*void CIRTDotOutputVisitor::Visit( IRTEBinop * binop )
	{
		size_t id = enterNode( "IRTEBinop" );
		addChild( id, binop->GetLeft() );
		addChild( id, binop->GetRight() );
		//TODO operation
	}*/
}